# Legacy University Dashboard (Deprecated)

This route group is kept only for compatibility while legacy migration completes.

- Active roles: `student`, `corporate`, `admin`
- Current behavior: users are redirected by `layout.tsx` to `/dashboard`
- Do not add new product features here
- Any remaining references should move into `services/legacy` and `lib/api/legacyUniversity.ts`
